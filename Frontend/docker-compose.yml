version: '3.8'
services:
  front:
    build:
      context: .
      dockerfile: Dockerfile_react
    restart: always
    volumes:
      - ./node:/usr/local/app:cached
    command: sh -c "cd frontend && yarn start"
    ports:
      - "3000:3000"